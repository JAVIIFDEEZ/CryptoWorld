# ─────────────────────────────────────────────────────────────────
# requirements.txt — Dependencias del backend CryptoWorld
#
# Organizado por categorías para facilitar mantenimiento.
# Fijar versiones exactas garantiza reproducibilidad.
# ─────────────────────────────────────────────────────────────────

# ── Framework principal ───────────────────────────────────────────
Django==5.0.6
djangorestframework==3.15.2

# ── Base de datos ─────────────────────────────────────────────────
psycopg2-binary==2.9.9          # Driver PostgreSQL para Python

# ── Autenticación JWT ─────────────────────────────────────────────
djangorestframework-simplejwt==5.3.1

# ── Autenticación 2FA (TOTP — Google Authenticator) ──────────────
pyotp==2.9.0                    # Generación y verificación de TOTP
qrcode==7.4.2                   # Generación de QR codes
Pillow==10.4.0                  # Dependencia de qrcode para PNG

# ── CORS ──────────────────────────────────────────────────────────
django-cors-headers==4.4.0

# ── Variables de entorno ──────────────────────────────────────────
python-dotenv==1.0.1

# ── HTTP client (Binance & CoinGecko) ─────────────────────────────
requests==2.32.3

# ── Análisis técnico y Machine Learning ───────────────────────────
pandas==2.2.2
numpy==1.26.4
ta==0.11.0                      # Technical Analysis library (indicadores)
scikit-learn==1.5.1             # ML para predicción de precio

# ── Testing ───────────────────────────────────────────────────────
pytest==8.2.2
pytest-django==4.8.0
pytest-cov==5.0.0               # Cobertura de código
factory-boy==3.3.1              # Generación de datos de prueba

# ── Servidor de producción ────────────────────────────────────────
gunicorn==22.0.0

# ── Celery — Worker asíncrono para alertas y tareas periódicas ─────
celery==5.4.0
redis==5.0.8                    # Cliente Redis para Python (broker/backend)
django-celery-beat==2.6.0       # Scheduler persistente en BD para celery beat
