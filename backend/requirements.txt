# ─────────────────────────────────────────────────────────────────
# requirements.txt — Dependencias del backend CryptoWorld
#
# Organizado por categorías para facilitar mantenimiento.
# Fijar versiones exactas garantiza reproducibilidad.
# ─────────────────────────────────────────────────────────────────

# ── Framework principal ───────────────────────────────────────────
Django==5.0.6
djangorestframework==3.15.2

# ── Base de datos ─────────────────────────────────────────────────
psycopg2-binary==2.9.9          # Driver PostgreSQL para Python

# ── Autenticación JWT ─────────────────────────────────────────────
djangorestframework-simplejwt==5.3.1

# ── Autenticación 2FA (TOTP — Google Authenticator) ──────────────
pyotp==2.9.0                    # Generación y verificación de TOTP
qrcode==7.4.2                   # Generación de QR codes
Pillow==10.4.0                  # Dependencia de qrcode para PNG

# ── CORS ──────────────────────────────────────────────────────────
django-cors-headers==4.4.0

# ── Variables de entorno ──────────────────────────────────────────
python-dotenv==1.0.1

# ── HTTP client (Binance & CoinGecko) ─────────────────────────────
requests==2.32.3

# ── Análisis técnico y Machine Learning ───────────────────────────
pandas==2.2.2
numpy==1.26.4
ta==0.11.0                      # Technical Analysis library (indicadores)
scikit-learn==1.5.1             # ML para predicción de precio
scipy==1.13.1                   # Estadística para la suite de robustez (DSR, tests)
optuna==3.6.1                   # Optimización de hiperparámetros (walk-forward, PBO)

# ── Testing ───────────────────────────────────────────────────────
pytest==8.2.2
pytest-django==4.8.0
pytest-cov==5.0.0               # Cobertura de código
factory-boy==3.3.1              # Generación de datos de prueba

# ── Servidor de producción ────────────────────────────────────────
gunicorn==22.0.0
# Workers ASGI para gunicorn. El proyecto usa Channels (WebSockets de precios y
# notificaciones): servir config.wsgi los deja muertos en producción, porque
# WSGI no sabe hacer el upgrade del protocolo. Con UvicornWorker se conserva la
# gestión de procesos de gunicorn y se sirve config.asgi.
uvicorn[standard]==0.30.6
dj-database-url==2.2.0          # Parsea DATABASE_URL (Railway lo inyecta automáticamente)
whitenoise==6.7.0               # Sirve archivos estáticos sin Nginx (Railway no tiene Nginx)

# ── Email transaccional ───────────────────────────────────────────
django-anymail[sendgrid]==10.3  # Integración API nativa SendGrid (mejor que SMTP puro)

# ── Celery — Worker asíncrono para alertas y tareas periódicas ─────
celery==5.4.0
redis==5.0.8                    # Cliente Redis para Python (broker/backend)
django-celery-beat==2.6.0       # Scheduler persistente en BD para celery beat

# ── Tiempo real — WebSockets vía Django Channels (ASGI) ────────────
channels==4.3.2                 # Capa ASGI + consumers WebSocket
channels-redis==4.3.0           # Channel layer sobre Redis (broadcast multi-proceso)
daphne==4.2.2                   # Servidor ASGI (runserver WS en dev + producción)

# ── Trading real — conexión multi-exchange ─────────────────────────
ccxt==4.5.63                    # API unificada de exchanges (Binance, Kraken, Coinbase…)
cryptography>=41.0.7            # Cifrado Fernet de las claves API por usuario
pywebpush==2.0.0  # Web Push (PWA); opcional — sin él las notificaciones push se desactivan solas
